captured
codex -c model_provider=tack_provider -c 'model_providers.tack_provider.name="tack loopback"' -c 'model_providers.tack_provider.base_url="http://127.0.0.1:8941/v1"' -c 'model_providers.tack_provider.env_key="FAKE_API_KEY"' -c 'model_providers.tack_provider.wire_api="responses"' exec --json --model fake-model --skip-git-repo-check --approve-for-me "write a file"
env: HOME, XDG_CONFIG_HOME, CODEX_HOME, OPENAI_API_KEY, FAKE_API_KEY
2026-09-20

The fake server's tool call carried `{"cmd": "touch /capture/outside-approve.txt"}` (a path
outside the git workspace codex was run in; the real capture used a `/var/tmp/tack-measure`
scratch path, rewritten to `/capture` here per the redaction rule, though it never appears in
this particular stdout file). No approval prompt appeared; the call proceeded and failed
inside the sandbox `--approve-for-me` still enforces (`workspace-write`, per its own
`--help` text): `touch: cannot touch '...': Read-only file system`, exit 1 (confirmed from
the fake server's logged `function_call_output`).
