# Relative base so the SPA runs same-origin and the dev server proxies /api to
# the tack-api server (see vite.config.ts). This avoids CORS in development and
# matches production, where the API binary serves the SPA from the same origin.
VITE_API_URL=/api
