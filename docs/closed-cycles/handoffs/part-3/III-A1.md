# III-A1 handoff

- Original base SHA / branch: `1d71785` / `agent/iii-a1-trust`. Redirect/WebSocket audit follow-up base SHA / branch / final SHA: `bf7aec2` / `agent/iii-a1-wave0-followup` / recorded by the commit containing this handoff.
- Files changed (must equal ownership list): `crates/tack-api/src/{config,middleware,router,server,webhook,github_sync}.rs`, `crates/tack-api/src/handlers/import_github.rs`, focused API security tests; `frontend/src/shared/ui/RichTextEditor.tsx`, frontend token stores, `boardSocket.ts`, and their focused tests; this handoff.
- Contract fixtures consumed: none; this Wave 0 security repair does not alter runner-v1 fixtures.
- Behavior implemented: persisted rich text is allow-list sanitised before it reaches the editor and before it is saved; CSP blocks executable/object content; API auth exemptions are exact; tokens are origin-bound, session-only browser credentials; WebSockets derive their endpoint from the configured API base and authenticate with a non-query subprotocol; query strings are omitted from request spans; startup rejects unauthenticated non-loopback binds and insecure Alexa configuration; config file defaults are merged before environment overrides.
- Tests added and exact commands/results: original focused commands remain recorded above; follow-up passed `cargo test -p tack-api redirect_does_not -- --nocapture` (2 lib redirect tests plus the GitHub-import integration test), `cargo test -p tack-api --test trust_boundary_test split_origin_websocket_handshake_accepts_subprotocol_credential_without_query_token -- --exact` (1 full TCP upgrade test), `cargo test -p tack-api --test trust_boundary_test` (3 tests), and `npm test -- --run src/shared/realtime/boardSocket.test.ts` (10 frontend tests). `cargo fmt --all --check` and `git diff --check` also passed. The Rust commands required the sandbox's approved loopback-listener permission; the isolated worktree had no frontend `node_modules`, so the unchanged frontend test ran against the shared checkout's installed dependencies.
- Failure/adversarial case proved: script/event-handler markup is stripped; suffix lookalikes remain bearer-protected; an invalid WebSocket subprotocol token fails; an origin-suffix lookalike cannot match the configured origin; unsafe non-loopback startup is rejected. Follow-up: 302 responses cannot retarget a signed webhook, GitHub sync bearer token, or user-supplied GitHub import PAT to a second (private/loopback fixture) server; each test asserts the target received zero requests. A real split-origin WebSocket HTTP upgrade succeeds with only `Sec-WebSocket-Protocol: tack.v1, tack.auth.c2VjcmV0LXRva2Vu`, no query string and no `Authorization` header.
- Schema/API/contract change requested from another owner: no schema/OpenAPI changes. All A1-owned outbound Webhook/GitHub reqwest clients now use `Policy::none()`; a 3xx result is surfaced as a non-success response and no credential-bearing request is followed.
- Known limitations or `not_measured` fields: no durable browser-login/session endpoint exists; the safe strategy is intentionally a session-scoped bearer token entered by the operator. The handshake evidence is a raw, browser-valid HTTP upgrade rather than a graphical-browser E2E run; the frontend unit test separately verifies that the production client supplies the same subprotocol credential channel.
- Secrets/logging review: browser credentials are never persisted in localStorage; WebSocket credentials are not query parameters and are never echoed as the negotiated subprotocol; request spans record only `path`, not query strings.
- Safe merge order and likely conflicts: merge after/with A0 baseline; likely conflicts only in A1-owned API boundary files and the shared frontend API/socket files.
- Checklist: no unowned files, no live secret, no panic stub, no blind retry.
