# IX-M8-runner handoff

- Base SHA / branch / final SHA: `c9339ee` (develop tip at dispatch, IX-M8-dedup already
  integrated) / `agent/ix-m8-runner` / `ca1c3f6`
- Files changed (must equal ownership list): 8 test files under `crates/tack-runner/src/**/tests.rs`
  (`engine`, `harness`, `harness/artifact`, `harness/claude_code`, `harness/codex`,
  `provider/vercel_ai_gateway`, `secrets`, `transport`) and 6 under `crates/tack-runner/tests/`
  (`bootstrap_entrypoint.rs`, `crash_matrix.rs`, `g2_journal_corruption_test.rs`,
  `h3_checkout.rs`, plus the two inline-`mod tests` files `harness/redact.rs` and
  `provider/anthropic.rs` from the inherited commits) — every file is inside this card's
  ownership (`crates/tack-runner/tests/**` and `crates/tack-runner/src/**/tests.rs`). No
  production file, no `TODO.md`, no `scripts/maintainability.py` or its baseline touched.
  `docs/adr/0064-fixed-waits.txt` was regenerated by the inherited commits, header preserved.
- Contract fixtures consumed: `docs/contracts/runner-v1/*.json` (frozen request/response
  bodies transport/tests.rs and engine/tests.rs assert against) — read only, byte-for-byte
  unchanged, none touched by this card.
- Behavior implemented: none. Every commit on this branch is test-only restructuring
  (rename, table-drive, extract-helper, split-multi-claim-test, remove-dead-cleanup);
  `git diff --stat develop...HEAD` touches only test files, `docs/adr/0064-fixed-waits.txt`,
  and this handoff.
- Tests added and exact commands/results: net +3 tests in tack-runner (242 -> 245): one
  from splitting a two-claim codex artifact test, two from splitting a two-claim
  claude_code secret-reference test and a two-claim secret-shim test (net effect of three
  splits minus none removed). `cargo nextest run --workspace -E 'package(tack-runner)'` ->
  `245 tests run: 245 passed, 6 skipped`.
- Failure/adversarial case proved: this card doesn't add new behavior to adversarially
  test, but its own process failed once and was caught: a mid-refactor split of
  `harness/codex/tests.rs`'s `run_fake_binary_success()` returned only the outcome, not the
  `TempDir` guard the staged artifact file lives under; the guard dropped (deleting the
  file) before the second split-off test read it back, and
  `fake_binary_success_stages_the_output_artifact` failed on `No such file or directory`.
  Fixed by returning `(HarnessOutcome, tempfile::TempDir)` and holding the guard in both
  callers — see commit `a576c3f`'s body and *What was removed* row for
  `run_fake_binary_success`.
- Schema/API/contract change requested from another owner: none.
- Known limitations or `not_measured` fields: two files remain over the 1 000-line
  acceptance line after an honest table-driving/dedup pass — `engine/tests.rs` (2 587) and
  `harness/claude_code/tests.rs` (1 344) — and two test bodies in
  `harness/claude_code/tests.rs` remain over the 40-line target (41, 43; both well under
  the 60-line hard cap). All three are reported as **unmet acceptance lines**, not "not
  worsened" — see *Budget check* and the file-size note in *What was removed*.
- Secrets/logging review: N/A — this card touches test structure only. The one test whose
  whole purpose is a secrets/logging assertion
  (`secret_resolution_is_logged_by_name_never_by_value`, split off from
  `secret_reference_resolves_and_only_length_reaches_the_shim`) keeps its exact original
  assertions: the log names the entry, never the resolved value.
- Safe merge order and likely conflicts: no ordering constraint against the other Wave 32
  `IX-M8-<crate>` cards — this card touches only `tack-runner`. Within `tack-runner`,
  `docs/adr/0064-fixed-waits.txt` is a generated file routed through the `tack-generated`
  merge driver; regenerate once at the end of the wave rather than trusting any one
  branch's copy, per `~/.../TODO.md` §VI.3 convention this repo follows for shared files.
- Checklist: no unowned files touched; no live secret; no panic stub; no blind retry.

## Resumption note

This attempt resumed an earlier one that was cut off mid-way by an API limit, having left
8 commits (`9434e76`..`8fc20eb`) and one uncommitted, non-compiling edit in
`transport/tests.rs` (a test split with the `#[tokio::test]` attribute left on the wrong
function and the wrong trait bound on the extracted helpers — `AttemptDataProtocol`'s
methods called through a `PullProtocol` bound). All 8 inherited commits were verified
against `git diff --stat develop...HEAD` (test files and `0064-fixed-waits.txt` only, no
production code) and kept as-is; none were reverted or redone. The uncommitted edit was
finished (fixed the attribute and trait bound, then continued the same rule-1/rule-3 pass
to bring the file's other over-length bodies down and the whole file under 1 000 lines)
rather than reverted, since the split itself — necessary to clear a 113-line body — was
sound; only its two mechanical mistakes needed fixing.

## Claim → evidence

| Claim (user-visible, added or kept) | Evidence — command, test name, or transcript |
|---|---|
| Every `tack-runner` test still proves what it did before | `cargo nextest run --workspace -E 'package(tack-runner)'` -> `245 tests run: 245 passed, 6 skipped`, run repeatedly across the whole card with no failure |
| `duplicate-tests` for the crate is 0 pairs | `python3 scripts/maintainability.py duplicate-tests crates/tack-runner` -> `0 pairs in total` (one new pair briefly appeared mid-card from a codex split colliding by name with an existing claude_code test — see commit `f045dc2` — and was resolved by rename before landing) |
| No fixed wait (>=200ms) remains in `tack-runner` test code | `python3 scripts/list-fixed-waits.py` lists 10 entries, all in `tack-orch`/`tack-api`; none in `tack-runner` (base had one, `secrets/tests.rs:161` at 5000ms, removed by the inherited `9434e76`) |
| `cargo llvm-cov -p tack-runner --fail-under-lines 85` still passes, coverage not weakened | `91.55%` after, identical to the `91.55%` base measurement (`cargo llvm-cov -p tack-runner --summary-only`, TOTAL line column) |
| Every test name in the crate is <=60 characters | `measure --json crates/tack-runner`: `test_name_max_chars` is <=60 in all 24 test files with tests (max is exactly 60 in several, none over) |
| No test body over 60 lines anywhere (hard cap) | `measure --json crates/tack-runner`: max `test_fn_max_lines` across the crate is 43 (`harness/claude_code/tests.rs`) |

A row with no evidence is a claim to delete, not a row to leave blank.

## Measured numbers

See *Budget check* below for the full before/after tables. Headline: workspace
`test_to_prod_ratio` 1.281 -> 1.272; `tack-runner` alone: 13 358 -> 12 885 test lines
(-473, despite +3 tests) with `test_fn_max_lines` down from a crate-wide max of 125
(`harness/tests.rs`, base) to 43 (`harness/claude_code/tests.rs`, final).

## What a stranger still cannot do

A stranger reading `engine/tests.rs` or `harness/claude_code/tests.rs` still cannot tell,
from the file alone, that their line count is an accepted, reasoned exception rather than
an oversight — that judgment lives only in this handoff and the two files' own commit
messages (`022fe53`, `ca1c3f6`), not in a comment in the files themselves (per this repo's
own comment rule, board/card reasoning does not belong in source comments). A future IX-M9
pass tightening the file-size budget will need to re-read this handoff's *What was
removed* to know what has already been tried (three dedup levers, all exhausted) before
concluding the remaining bulk is prunable.

## Budget check

`python3 scripts/maintainability.py check --changed` (final tree, clean working copy):
```
✓ maintainability budgets hold (0 files checked)
```

`python3 scripts/maintainability.py measure --totals`:
```
before (c9339ee): totals: prod=55599 (comments 11144) test=71211 ratio=1.281 tests=1408 sleeps_in_tests=38 env_gated=0
after  (ca1c3f6): totals: prod=55599 (comments 11144) test=70738 ratio=1.272 tests=1411 sleeps_in_tests=37 env_gated=0
```

`python3 scripts/maintainability.py measure --json crates/tack-runner` — per file, before
(`c9339ee`) -> after (`ca1c3f6`), lines / tests / max body / max name:

| File | Before | After |
|---|---|---|
| `src/config/tests.rs` | 218 / 9 / 36 / 78 | 218 / 9 / 36 / 60 |
| `src/engine/tests.rs` | 3032 / 40 / 102 / 88 | 2587 / 40 / 40 / 60 |
| `src/git/tests.rs` | 738 / 20 / 39 / 63 | 738 / 20 / 39 / 59 |
| `src/harness/artifact/tests.rs` | 192 / 5 / 43 / 50 | 179 / 5 / 40 / 50 |
| `src/harness/claude_code/tests.rs` | 1299 / 18 / 66 / 81 | 1344 / 21 / 43 / 60 |
| `src/harness/codex/tests.rs` | 884 / 13 / 51 / 88 | 909 / 14 / 38 / 60 |
| `src/harness/event_sink/tests.rs` | 197 / 6 / 33 / 67 | 197 / 6 / 33 / 60 |
| `src/harness/process/tests.rs` | 401 / 9 / 47 / 67 | 374 / 9 / 39 / 60 |
| `src/harness/tests.rs` | 1000 / 17 / 125 / 90 | 994 / 17 / 39 / 60 |
| `src/journal/tests.rs` | 162 / 5 / 33 / 74 | 162 / 5 / 33 / 59 |
| `src/provider/tests.rs` | 349 / 8 / 87 / 89 | 360 / 8 / 36 / 57 |
| `src/provider/vercel_ai_gateway/tests.rs` | 246 / 5 / 53 / 88 | 260 / 5 / 37 / 58 |
| `src/secrets/tests.rs` | 189 / 11 / 25 / 61 | 193 / 11 / 25 / 57 |
| `src/transport/tests.rs` | 1030 / 24 / 113 / 76 | 965 / 24 / 40 / 59 |
| `src/workspace/tests.rs` | 298 / 8 / 36 / 68 | 298 / 8 / 36 / 57 |
| `tests/bootstrap_entrypoint.rs` | 180 / 2 / 53 / 60 | 180 / 2 / 35 / 60 |
| `tests/cli.rs` | 29 / 2 / 12 / 65 | 29 / 2 / 12 / 58 |
| `tests/crash_matrix.rs` | 826 / 6 / 56 / 58 | 820 / 6 / 40 / 58 |
| `tests/g2_journal_corruption_test.rs` | 169 / 3 / 55 / 58 | 161 / 3 / 38 / 58 |
| `tests/h3_checkout.rs` | 664 / 6 / 55 / 60 | 662 / 6 / 35 / 60 |

(`harness/redact.rs` and `provider/anthropic.rs` carry an inline `mod tests` each, not a
`tests.rs` file, so `measure`'s file-list scan doesn't surface them individually; both were
name-only edits, see *What was removed*. `tests/live/*.rs` and `tests/common/mod.rs` are
unchanged — 0 tests reported is `measure`'s handling of `#[ignore]`d/helper-only files, not
a gap.)

**Acceptance status, per the card's three size/shape lines:**
- No body over 40 outside named exclusions, none over 60 anywhere: **met everywhere except**
  `harness/claude_code/tests.rs` (41, 43 — both under the 60 hard cap; unmet on the interim
  gate's own harder 40-line target, reported here rather than silently left).
- No name over 60 characters: **met**, crate-wide (24 test files with tests all report
  `test_name_max_chars` <= 60).
- No file over 1 000 lines outside named exclusions: **met** everywhere except
  `engine/tests.rs` (2 587) and `harness/claude_code/tests.rs` (1 344) — both **unmet**, with
  the reasoning in *What was removed* below.

`duplicate-tests crates/tack-runner`: `0 near-identical pairs across files (ratio > 0.75)`
— unchanged from the pre-card state IX-M8-dedup established (one new pair surfaced
mid-card and was resolved before landing, see *Claim -> evidence*).

`cargo llvm-cov -p tack-runner --fail-under-lines 85 --summary-only`: exit 0, TOTAL line
coverage **91.55%** (identical to the base measurement on `c9339ee`, `after >= before`
satisfied with equality — no test's coverage contribution was lost by any merge or split).

`cargo nextest run --workspace -E 'package(tack-runner)'`:
```
Starting 245 tests across 8 binaries (6 tests and 31 binaries skipped)
Summary [   0.55s] 245 tests run: 245 passed, 6 skipped
```

`cargo fmt --all` (workspace-wide, not just `-p tack-runner`): clean, no diff.

`./scripts/check-comments.sh && ./scripts/check-test-hygiene.sh`:
```
✓ no board archaeology in crates/ frontend/src frontend/e2e
✓ tests take their temporary paths from a guard
```

`cargo clippy --workspace --all-targets -- -D warnings`: clean, `Finished` with no
warnings printed.

`python3 scripts/list-fixed-waits.py`: 10 entries, all `tack-orch`/`tack-api`; none in
`tack-runner`.

## What was removed

One line per removed, merged, or split test, or moved comment block, tagged with the
plan §2.2 rule number. The eight inherited commits (`9434e76`..`8fc20eb`) are included —
this card verified rather than redid them.

**`src/secrets/tests.rs`** (`9434e76`, inherited):
- **`8: fixed wait`** — `bounded_times_out_promptly`'s 5 000 ms `std::thread::sleep`
  simulating a hung probe replaced with an unfulfilled channel `recv()`, which `bounded()`
  already abandons on timeout via `mpsc::recv_timeout` — same behavior proved, zero
  duration. This was the crate's only entry in `0064-fixed-waits.txt`.

**`src/engine/tests.rs`** (`33f2399`, `997612b`, `c193a9d`, `3f6a69a`, this card's `022fe53`):
- **`3: share`** — `workspace_manager()`, `tracked_workspace_manager()`, `prepared_record()`,
  `claimed_record()` (inherited, `33f2399`): collapsed ~50 call sites' identical 7-8 line
  `WorkspaceManager`/`AttemptJournal` construction to one line each.
- **`3: share`** — `runner_engine()`/`runner_engine_with_clock()` (inherited, `997612b`):
  collapsed ~35 call sites' identical 4-6 line `RunnerEngine::new`/`with_clock` construction.
- **`1: variants -> rows`** — merged `completion_response_loss_stays_in_terminal_outbox_without_duplicate_send`
  and `cancellation_response_loss_stays_in_terminal_outbox_without_duplicate_send` into
  `assert_outbox_replays_exact_payload_after_response_loss` parameterized on a new
  `TerminalKind`, called from two one-line wrappers (inherited, `c193a9d`); same for the
  ack-then-journal-failure pair.
- **`3: share`** — five assertion/setup extractions inflating single-scenario bodies past
  the cap (inherited, `c193a9d`): start/cancellation-report field checks, the
  quarantine-directory probe (3 call sites), the staged-artifact fixture, the
  terminal-event-submitted check, the pre-spawn-rejection `ActualExecution` shape.
- **`2: name`** — 24 names over 60 characters (up to 88) shortened to the same claim
  (inherited, `3f6a69a`).
- **`3: share`** (this card) — `fresh_journal()` (32 call sites),
  `default_clock_engine()`/`clock_fixed_at_12_20_15()`/`clock_advancing_from_12_20_15()`
  (4 duplicated clock constructions), `journal_with_fake_checkout()` (2 call sites),
  `persisted_prepared_record()` (7 recovery tests), `single_completion_report()`,
  `default_completion_terminal_reason()` (3 sites).
- **`3: share`, single-use** (this card) — `assert_single_heartbeat_matches_fixture`,
  `assert_prepared_recovery_observation`, `assert_recovery_stays_pending` extracted from
  three test bodies that were otherwise over 40 lines with no second call site to justify
  a "shared" label; still legitimate per rule 3's body-length purpose even at one use.
- No test removed anywhere in this file across all six commits; no crash/recovery/replay
  scenario merged into another. **Unmet line:** file is 2587 lines (target 1000). The
  remaining bulk is three fake protocol/adapter/worktree/data-protocol implementations
  (`FakeProtocol` alone covers every protocol call and failure mode) plus 40 tests each
  pinning one crash/recovery/replay claim the crate's 85% coverage floor depends on.
  Getting under 1000 would require merging distinct claims into one test or moving content
  to another file, both forbidden by plan §5's row for this card ("never by splitting one
  file into two of the same content").

**`src/harness/tests.rs`** (`8fc20eb` inherited, this card's `022fe53`, `ca1c3f6`):
- **`1: variants -> rows` / `3: share`** — the cross-adapter body-length work in earlier
  commits pushed this file to 999-1005 lines at various points; consolidated further
  (inherited `8fc20eb`): `the_same_fixture_completes_through_both_real_adapters` reuses
  `real_adapters_for()` instead of hand-building both adapters again; an orphaned duplicate
  doc comment removed; three verbose doc blocks condensed without dropping a stated fact.
- **`3: share`** (this card) — `assert_cancel_kills_descendant_tree()` extracted from
  `both_adapters_route_cancel_to_kill_the_whole_descendant_tree`'s per-adapter loop body
  (45 -> ~10 counted lines); `assert_reconcile_pidless_cases()` extracted similarly from
  `reconcile_reports_shared_pid_plumbing_identically_for_both`.
- **`2: name`** (this card) — a `ReconcileCase<'a>` type alias replaces an inline tuple
  type in a case-table declaration, keeping the line under clippy's preferred width after
  two named closures were inlined.
- Final: 994 lines, max body 39, max name 60 — **fully meets acceptance**, from a base
  that was already at the 1000-line ceiling with a 125-line max body.

**`src/harness/artifact/tests.rs`** (this card):
- **`3: share`, dead-code removal** — 5 redundant `fs::remove_dir_all(workspace/staging)`
  tail calls removed; each path comes from this file's own `temp_dir()` `TempDir` guard,
  already removed on drop including on panic. No assertion changed.
- Final: 179 lines, max body 40, max name 50 — **fully meets acceptance**.

**`src/harness/claude_code/tests.rs`** (this card):
- **`3: share`** — moved two long inline rationale comments in
  `reconcile_reports_running_for_a_still_running_harness` into a `///` doc comment above
  the test attribute (doc comments before the `fn` line aren't counted toward body length);
  extracted `secret_length_dump_binary()`/`recorded_length()` helpers mirroring the
  existing `env_name_dump_binary()`/`recorded_env_names()` pair, to clear the file's other
  hard-cap (>60) violation.
- **`3: share`, dead-code removal** — 10 redundant `fs::remove_dir_all(workspace)` tail
  calls removed (same pattern/reasoning as `harness/artifact/tests.rs` above).
- **`1: variants -> rows`-adjacent, one-claim-per-test** — split
  `validate_rejects_a_missing_secret_reference_untouched` (59 lines, two claims: typed
  rejection reason, and workspace/store left untouched) into
  `validate_fails_with_a_typed_reason_for_a_missing_secret` and
  `a_rejected_validate_touches_neither_workspace_nor_store`, sharing
  `attempt_validate_with_missing_secret_reference()`.
- **one-claim-per-test** — split `secret_reference_resolves_and_only_length_reaches_the_shim`
  (49 lines, two claims: the shim only ever sees the length, and resolution is logged by
  name never by value) into that test (now just the length claim) and
  `secret_resolution_is_logged_by_name_never_by_value`, sharing
  `run_with_resolved_secret_reference()`.
- **`3: share`** — `run_provider_case()` extracted from
  `provider_endpoint_variables_present_only_when_configured`'s loop body.
- **`2: name`** — 46 names over 60 characters (up to 81, inherited `0ac452e`/`d07eb9d`)
  shortened; two of this card's own new split names landed at 63 on the first pass
  (`validate_rejects_a_missing_secret_reference_with_a_typed_reason`,
  `a_missing_secret_reference_leaves_workspace_and_store_untouched`) and were shortened
  again to 55/56 (`validate_fails_with_a_typed_reason_for_a_missing_secret`,
  `a_rejected_validate_touches_neither_workspace_nor_store`).
- No test removed; three real claims (typed-error mapping, workspace/store untouched,
  shim-length-only, log-name-not-value) went from being folded into two tests to being
  four, each single-claim. **Unmet lines:** file is 1344 lines (grew from splitting for
  body compliance; target 1000) and two bodies sit at 41/43 lines
  (`cancel_stops_the_process_and_forgets_its_bookkeeping_entry`,
  `reconcile_reports_running_for_a_still_running_harness`) — both single, cohesive
  integration scenarios (spawn a real fake-binary process, assert its lifecycle) with no
  second claim to split out; further restructuring looked like trimming lines to game the
  count rather than separating distinct claims, so left alone. Both are comfortably under
  the 60-line hard cap.

**`src/harness/codex/tests.rs`** (this card):
- **one-claim-per-test** — split `fake_binary_success_completes_with_output_and_artifact`
  (52 lines, two claims: outcome/model/usage, and artifact staging) into
  `fake_binary_success_reports_model_and_usage` and (after a rename, see below)
  `a_successful_codex_exec_stages_its_output_artifact`, sharing `run_fake_binary_success()`.
  First draft of the helper returned only the `HarnessOutcome`, dropping the artifact
  `TempDir` guard before the second test could read the staged file back — fixed by
  returning `(HarnessOutcome, tempfile::TempDir)` (see *Failure/adversarial case proved*).
- **`3: share`** — `assert_exec_case_outcome()` extracted from the table-driven
  `wait_classifies_terminal_state_from_the_exit_code_alone` loop body;
  `run_provider_env_case()` extracted from
  `provider_endpoint_credential_reaches_process_only_when_named`'s loop body.
  `start_and_wait()` (a pre-existing but until-now-uncalled helper surfaced as dead code by
  this refactor) wired into every call site that discarded its handle.
- **`2: name` / dedup-adjacent** — `a_successful_codex_exec_stages_its_output_artifact` is
  the renamed form of `fake_binary_success_stages_the_output_artifact`: the split's new
  name scored a 0.83 similarity ratio against `claude_code/tests.rs`'s
  `fake_binary_success_stages_a_real_log_artifact` (over the 0.75 `duplicate-tests`
  threshold) despite being a legitimately distinct per-adapter test — same resolution
  IX-M8-dedup already used for this exact shape of pair (rename, don't merge).
- Final: 909 lines, max body 38, max name 60 — **fully meets acceptance**.

**`src/provider/vercel_ai_gateway/tests.rs`** (this card):
- **`3: share`** — `assert_catalog_and_claude_base()` and `assert_full_override_wiring()`
  extracted from `the_test_only_base_url_override_rebases_catalog_and_wires`'s three
  sequential phases (unset / loopback-override / non-loopback-ignored), each phase now
  calling the matching helper with a phase label instead of a bespoke inline assertion.
- Final: 260 lines, max body 37, max name 58 — **fully meets acceptance**.

**`tests/bootstrap_entrypoint.rs`** (this card):
- **`3: share`** — a 26-line rationale comment inside
  `composition_root_stops_on_injected_shutdown_without_a_signal` moved to a `///` doc
  comment above the test attribute.
- Final: 180 lines, max body 35, max name 60 — **fully meets acceptance**.

**`tests/crash_matrix.rs`** (this card):
- **`3: share`** — `ambiguous_ack_scenario()` and `attempt_run_once()` extracted, each
  reused by 3 tests (`spawn_ack_loss_quarantines_as_ambiguous_without_retry`,
  `failed_ambiguity_report_retries_once_then_quarantines`,
  `reoffered_quarantined_attempt_rejected_before_second_spawn`).
- No scenario removed; all 6 crash/recovery scenarios present and independently verified
  (worktree-crash-before-spawn, spawn-ack-loss, terminal-report-loss (already
  table-driven), ambiguity-retry-then-quarantine, running-process-recovery,
  reoffered-quarantined-attempt).
- Final: 820 lines, max body 40, max name 58 — **fully meets acceptance**.

**`tests/g2_journal_corruption_test.rs`** (this card):
- **`3: share`** — `fresh_journal()` and `persist_all()` extracted, used by all 3 tests;
  dead-code removal of a `let _ = std::fs::remove_dir_all(root);` tail statement in all 3
  (same reasoning as the `artifact`/`claude_code` cleanup removals above).
- Final: 161 lines, max body 38, max name 58 — **fully meets acceptance**.

**`tests/h3_checkout.rs`** (this card):
- **`3: share`** — `git_engine()` extracted, reused by 5 of 6 tests (the sixth uses
  `UnavailableWorktreeProvisioner`, a different provisioner type, so it does not fit the
  shared constructor); `crash_before_terminal_report()` extracted, reused by
  `a_checkout_left_by_a_killed_runner_is_removed_by_the_restart` and
  `a_quarantined_attempt_keeps_its_checkout_as_evidence`.
- Final: 662 lines, max body 35, max name 60 — **fully meets acceptance**.

**`src/config/tests.rs`, `src/git/tests.rs`, `src/harness/event_sink/tests.rs`,
`src/journal/tests.rs`, `src/provider/tests.rs`, `src/secrets/tests.rs`,
`src/workspace/tests.rs`, `tests/cli.rs`, `src/harness/redact.rs`'s and
`src/provider/anthropic.rs`'s inline `mod tests`** (inherited, `0ac452e`/`d07eb9d`):
- **`2: name`** only — 70 names total (68 + 2 in the inline modules) over 60 characters
  (up to 90) shortened to the same claim; one rename collided by similarity with an
  existing name and was disambiguated (`both_adapters_route_cancel_to_kill_the_whole_descendant_tree`).
  No body, file-size, or fixed-wait issue in any of these files at base or after — they
  needed only the name pass.

## Context spent

- Tokens read before the first edit: `git status`/`git diff`/`git log --stat` on the
  inherited state (~3k), `TODO.md` §IX.0-§IX.3 and the IX-M8 card block (~3k), this
  crate's `docs/agent-handoffs/part-ix/README.md` header and card row (~1k), plan §2.2 and
  §5 row M8 (~1.5k), the TEMPLATE files (~0.5k), the IX-M4/IX-M5 *What was removed*
  sections for this crate (~2k) — in line with the dispatch README's read-list.
- Context size at handoff: large — this card's remaining scope after the inherited 8
  commits was still most of the crate's body-length work (11 of ~23 test files still over
  budget), split across four parallel sub-agents (one per `engine/tests.rs`, one for
  `harness/claude_code/tests.rs`'s hard-cap violations, one for a five-file harness/provider
  batch, one for a four-file integration-test batch) with the lead session doing
  `transport/tests.rs`, verification/integration of every batch, and the final
  claude_code/tests.rs cleanup pass itself.
- Files opened and not used: none beyond the read list and the files edited.
- Read-list lines that were wrong: the task's stated base coverage number (91.55%) matched
  this card's own direct measurement on `c9339ee` exactly; a different number (89.21%) seen
  in `IX-M8-dedup.md`'s own coverage-guard table for `tack-runner` was NOT the same
  measurement point (that table was captured mid-dedup-card, before some other Wave 32
  work landed on `develop`) and was not used as the base for this card's `after >= before`
  check — flagging in case a future reader cross-references the two documents and finds
  them apparently contradictory.

## Re-baselined?

`no`. `scripts/maintainability-baseline.json` has no diff:
```
$ git diff scripts/maintainability-baseline.json
(no output)
```

## Amendments

*(none yet)*
