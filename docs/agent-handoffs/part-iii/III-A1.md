# III-A1 handoff

- Base SHA / branch / final SHA: `1d71785` / `agent/iii-a1-trust` / recorded by the commit containing this handoff.
- Files changed (must equal ownership list): `crates/tack-api/src/{config,middleware,router,server}.rs`, focused API security tests; `frontend/src/shared/ui/RichTextEditor.tsx`, frontend token stores, `boardSocket.ts`, and their focused tests; this handoff.
- Contract fixtures consumed: none; this Wave 0 security repair does not alter runner-v1 fixtures.
- Behavior implemented: persisted rich text is allow-list sanitised before it reaches the editor and before it is saved; CSP blocks executable/object content; API auth exemptions are exact; tokens are origin-bound, session-only browser credentials; WebSockets derive their endpoint from the configured API base and authenticate with a non-query subprotocol; query strings are omitted from request spans; startup rejects unauthenticated non-loopback binds and insecure Alexa configuration; config file defaults are merged before environment overrides.
- Tests added and exact commands/results: `cargo test -p tack-api handlers::websocket::tests`; `cargo test -p tack-api middleware::tests`; `cargo test -p tack-api config::tests`; `cargo test -p tack-api --test trust_boundary_test`; `npm test -- --run src/shared/api/client.test.ts src/shared/realtime/boardSocket.test.ts src/shared/ui/RichTextEditor.test.ts`; `npm run type-check` — all passed.
- Failure/adversarial case proved: script/event-handler markup is stripped; suffix lookalikes remain bearer-protected; an invalid WebSocket subprotocol token fails; an origin-suffix lookalike cannot match the configured origin; unsafe non-loopback startup is rejected.
- Schema/API/contract change requested from another owner: no schema/OpenAPI changes. Redirect-following policy in the existing outbound `WebhookClient`/GitHub clients is outside A1 ownership; configuration now rejects credential-bearing/query-bearing outbound URLs, but a future owner should explicitly disable or revalidate response redirects before treating redirect hardening as complete.
- Known limitations or `not_measured` fields: no durable browser-login/session endpoint exists; the safe strategy is intentionally a session-scoped bearer token entered by the operator. Full WebSocket handshake integration needs a loopback listener, which this sandbox denies; protocol and route checks are focused/unit covered.
- Secrets/logging review: browser credentials are never persisted in localStorage; WebSocket credentials are not query parameters and are never echoed as the negotiated subprotocol; request spans record only `path`, not query strings.
- Safe merge order and likely conflicts: merge after/with A0 baseline; likely conflicts only in A1-owned API boundary files and the shared frontend API/socket files.
- Checklist: no unowned files, no live secret, no panic stub, no blind retry.
