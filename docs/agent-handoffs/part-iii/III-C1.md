# III-C1 handoff

- Base SHA / branch / final SHA: `f14019b` / `agent/iii-c1-operator-api` / recorded by the commit containing this handoff.
- Files changed (must equal ownership list): new `crates/tack-api/src/handlers/{executions,runner_admin}.rs`, focused `crates/tack-api/tests/c1_handlers_test.rs`, and this handoff.
- Contract fixtures consumed: `protocol.json`, `lifecycle-transitions.json`, cancellation and stable-error envelopes. IDs remain opaque and all responses use the v1 error envelope shape; no fixture changed.
- Behavior implemented: card-local operator routers for execution create/list/get/cancellation/requeue plus fleet, runner-revocation, agent-profile, and model-profile management. Creation validates Tack item existence, rejects unavailable/revoked exact runners, and delegates idempotency to B2's transactional enqueue. Cancellation records a request and does not claim a terminal state. Requeue is limited to the newest `needs_operator` attempt and writes an operator audit event in the same transaction.
- Tests added and exact commands/results: `cargo test -p tack-api --test c1_handlers_test` — 3 passed, covering duplicate replay, cancellation-as-requested, audited needs-operator-only recovery, and revoked-runner rejection. `cargo clippy -p tack-api --test c1_handlers_test -- -D warnings`, `cargo fmt --all --check`, and `git diff --check` passed.
- Failure/adversarial case proved: changed execution idempotency inputs conflict rather than silently merging; cancellation leaves request state queued; a `lost` attempt cannot be requeued; an accepted recovery emits exactly one `requeue_confirmed` audit event; revoked exact runner selection returns stable `runner_revoked`.
- Schema/API/contract change requested from another owner: **B2 amendment required.** The accepted schema has only `agent_runners.credential_hash`; it has no enrollment-token storage, one-time consumption marker, expiry/revocation fields, or repository methods. C1 therefore intentionally does not create an enrollment-token endpoint or unsafe JSON/in-memory substitute. This blocks the enrollment-token acceptance cases until B2 adds a durable, hash-only single-use token model/API.
- Known limitations or `not_measured` fields: these handlers are intentionally unregistered; C5 owns global router/OpenAPI wiring. List/detail DTOs are card-local and will need C5's generated-schema integration. Enrollment token issuance/consumption is blocked as above. Fleet membership is represented by B2's table but awaits a dedicated membership route in the C5 integration shape.
- Secrets/logging review: no credential or token is returned, logged, or stored by C1. Runner revocation mutates state only; enrollment credentials cannot be issued without the missing durable hash-only model.
- Safe merge order and likely conflicts: merge after B2, before C5. C5 should register the two `routes(OperatorExecutionState)` routers and own any OpenAPI/generated-schema reconciliation. No global router, handler registry, migration, repository, or fixture file was touched.
- Checklist: no unowned files, no live secret, no panic stub, no blind retry.
